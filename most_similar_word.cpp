#include<iostream>
#include<cmath>
using namespace std;
struct s
{
    char str[50];
}

int main()
{
    int t;
    cin>>t;
    while(t--)
    {
        s w[]
        int sm;
        sm=min[0];
        for (int i=0;i<n-1;i++)
        {
            if(sm>min[i])
            sm=min[i];
        }
        cout<<sm;
    }
}